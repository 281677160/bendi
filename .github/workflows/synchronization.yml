#
# 同步上游仓库或者清理发布都需要用REPO_TOKEN密匙
#
# 记住，同步上游是不会同步您任何自建文件夹的，所以同步后您要自己对比我现有的文件夹里面diy-part.sh和settings.ini文件有没有更新
#
# REPO_TOKEN密匙制作教程：https://git.io/jm.md
#


name: 同步上游仓库
on:
  workflow_dispatch:

env:
  BILDYML: synchronization.yml
  REPO_TOKEN: ${{ secrets.REPO_TOKEN }}
  TZ: Asia/Shanghai

jobs:
  del_runs:
    runs-on: Ubuntu-20.04
    name: 同步上游仓库
    
    steps:
    - name: 准备结束
      uses: actions/checkout@v2
      
    - name: 部署环境
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo -E apt-get -qq update > /dev/null 2>&1
        sudo -E apt-get -qq install -y git subversion git-core wget curl grep > /dev/null 2>&1
        sudo timedatectl set-timezone "$TZ"
        git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git config --global user.name "github-actions[bot]"
          
    - name: 整理数据
      run: |
        cd $GITHUB_WORKSPACE
        git clone -b main https://github.com/281677160/bendi.git shangyou
        git clone -b main https://github.com/281677160/bendi.git repogx
        
    - name: 同步上游仓库
      run: |
        cd $GITHUB_WORKSPACE
        rm -fr repogx/Lede_source/*
        rm -fr repogx/Lienol_source/*
        rm -fr repogx/Mortal_source/*
        rm -fr repogx/Tianling_source/*
        rm -fr repogx/openwrt_amlogic/*
        
        cp -Rf $GITHUB_WORKSPACE/shangyou/Lede_source/settings.ini repogx/Lede_source/settings.ini
        cp -Rf $GITHUB_WORKSPACE/shangyou/Lienol_source/settings.ini repogx/Lienol_source/settings.ini
        cp -Rf $GITHUB_WORKSPACE/shangyou/Tianling_source/settings.ini repogx/Tianling_source/settings.ini
        cp -Rf $GITHUB_WORKSPACE/shangyou/Mortal_source/settings.ini repogx/Mortal_source/settings.ini
        cp -Rf $GITHUB_WORKSPACE/shangyou/openwrt_amlogic/settings.ini repogx/openwrt_amlogic/settings.ini
        chmod -R 775 repogx
        cd repogx
        git add .
        git commit -m "up"
        git push --quiet "https://${{ secrets.REPO_TOKEN }}@github.com/${{github.repository}}" HEAD:main
